spring:
  # mysql DB ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://j7d103.p.ssafy.io:3306/samals?serverTimezone=Asia/Seoul
    username: root
    password: ALLCHUNG2

  # jpa ??
  jpa:
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
